import type { Props } from "$store/components/header/Searchbar.tsx";
import { SearchbarInput } from "$store/components/header/Searchbar.tsx";

function IslandSearchbarInput(props: Props) {
  return <SearchbarInput {...props} />;
}

export default IslandSearchbarInput;
