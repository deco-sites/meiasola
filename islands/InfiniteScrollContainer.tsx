import Component from "$store/components/search/InfiniteScrollContainer.tsx";
import type { Props } from "$store/components/search/InfiniteScrollContainer.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
